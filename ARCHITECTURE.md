# ARCHITECTURE.md

## Архитектура CNC-ASSISTANT / СТАНКО-ИИ

## Цель системы
Построить промышленного ИИ‑ассистента для ЧПУ, который:
- понимает человеческое описание задачи
- делает разумные предположения
- считает режимы резания
- проверяет физические ограничения
- собирает реальный опыт операторов
- накапливает знания из интернета
- с первого дня готовит датасет для обучения СОБСТВЕННОЙ LLM

LLM на старте НЕ требуется.  
Система должна быть полезной **до ИИ**, а не после.

---

## Общий поток данных
  cnc-assistant/
├── app/
│ ├── main.py
│ │ # Точка входа, инициализация всех сервисов
│
│ ├── bot/
│ │ ├── telegram_bot.py
│ │ │ # Telegram API, кнопки, сообщения
│ │ │
│ │ ├── dialogs.py
│ │ │ # Сценарии диалогов (ниже / выше / оставить)
│ │ │
│ │ └── handler.py
│ │ # Оркестратор: связывает парсер, контекст, калькулятор
│
│ ├── core/
│ │ ├── state_machine.py
│ │ │ # FSM: что уже известно, что можно предположить
│ │ │
│ │ ├── parser.py
│ │ │ # Парсер текста пользователя (материал, Ø, станок)
│ │ │
│ │ ├── assumptions.py
│ │ │ # Логика предположений:
│ │ │ # если не указано → разумно догадаться
│ │ │
│ │ ├── calculator.py
│ │ │ # Физика резания:
│ │ │ # Vc, n, ap, f, мощность
│ │ │
│ │ ├── pass_strategy.py
│ │ │ # Разбивка на проходы:
│ │ │ # черновая / получистовая / чистовая
│ │ │
│ │ └── validator.py
│ │ # Защита от бреда:
│ │ # превышение мощности, ап, оборотов
│
│ ├── domain/
│ │ ├── models.py
│ │ │ # Базовые сущности:
│ │ │ # Material, Tool, Machine, Operation
│ │ │
│ │ └── experience.py
│ │ # Модель опыта оператора
│ │ # (доверие, отклонения, стиль работы)
│
│ ├── services/
│ │ ├── recommendation.py
│ │ │ # Превращает расчёты в человеческий текст
│ │ │
│ │ ├── comparison.py
│ │ │ # Bot vs Operator (где человек умнее)
│ │ │
│ │ ├── data_collector.py
│ │ │ # Сохраняет решения операторов
│ │ │
│ │ └── knowledge_service.py
│ │ # Доступ к базе знаний и интернет-данным
│
│ ├── knowledge/
│ │ ├── internet_parser/
│ │ │ ├── sources.py
│ │ │ │ # Сайты, форумы, PDF, ГОСТы
│ │ │ │
│ │ │ ├── scraper.py
│ │ │ │ # Скачивание HTML / PDF
│ │ │ │
│ │ │ ├── text_cleaner.py
│ │ │ │ # Очистка мусора
│ │ │ │
│ │ │ └── extractor.py
│ │ │ # Извлечение фактов:
│ │ │ # материалы, Vc, ap, рекомендации
│ │ │
│ │ ├── normalizer/
│ │ │ ├── material_map.py
│ │ │ │ # 12Х18Н10Т = AISI 304 = нерж
│ │ │ │
│ │ │ ├── tool_map.py
│ │ │ │ # CNMG ↔ ISO ↔ народные названия
│ │ │ │
│ │ │ └── operation_map.py
│ │ │ # токарка / точение / обдирка
│ │ │
│ │ └── knowledge_base/
│ │ ├── materials.json
│ │ ├── tools.json
│ │ ├── machines.json
│ │ └── cutting_rules.json
│
│ └── storage/
│ ├── db.py
│ │ # БД решений операторов
│ │
│ └── data_pipeline.py
│ # Подготовка датасетов под LLM
│
├── data/
│ ├── rules/
│ │ # Табличные справочные значения
│ │
│ └── limits/
│ # Ограничения станков и инструментов
│
└── training/
├── datasets/
│ # Чистые JSON для обучения
│
├── prompts/
│ # Будущие системные промпты
│
└── finetune/
# Дообучение ТВОЕЙ LLM


---

## Ключевая философия

- ❌ Не LLM делает ИИ  
- ✅ ИИ = контекст + предположения + физика + опыт людей  
- LLM — только следующий слой

---

## Что у тебя есть уже с первого дня

- Рабочий бот
- Сбор реального опыта
- Собственная база знаний
- Чистый датасет
- Полный контроль
- Никаких апдейтов чужих нейросетей

---

## Итог

Ты строишь **ИИ-технолога**, а не «чат-бота».  
Эта архитектура спокойно масштабируется:
- сначала без LLM
- потом с локальной LLM
- потом с промышленным ИИ

Если хочешь — дальше можем:
- расписать `Context` как JSON
- сделать `parser.py` (реальный код)
- или спроектировать формат датасета под обучение

Ты идёшь **очень правильным путём**.