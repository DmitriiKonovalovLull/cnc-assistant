# ============================================================================
# CNC ASSISTANT - Все зависимости проекта
# ============================================================================
# Установка: pip install -r requirements.txt
# Для OCR (распознавание изображений): pip install -r requirements_ocr.txt
# ============================================================================

# ============================================================================
# ОСНОВНЫЕ ЗАВИСИМОСТИ (обязательные)
# ============================================================================

# Telegram Bot Framework
aiogram>=3.0.0,<4.0.0

# База данных (ORM)
sqlalchemy>=2.0.0,<3.0.0

# Загрузка переменных окружения из .env файла
python-dotenv>=1.0.0,<2.0.0

# Работа с YAML файлами (для стандартов ГОСТ/ОСТ)
pyyaml>=6.0.0,<7.0.0

# Веб-скрапинг для поиска информации в интернете
requests>=2.31.0,<3.0.0
beautifulsoup4>=4.12.0,<5.0.0
aiohttp>=3.9.0,<4.0.0
urllib3>=2.0.0,<3.0.0

# Тестирование
pytest>=7.4.0,<8.0.0
pytest-cov>=4.1.0,<5.0.0
# lxml опционально (для более быстрого парсинга HTML, но не обязательно)
# Установка: pip install lxml (может требовать компилятор на Windows)
# Если lxml не установлен, используется встроенный html.parser

# ============================================================================
# ОПЦИОНАЛЬНЫЕ ЗАВИСИМОСТИ (для расширенного функционала)
# ============================================================================

# OCR для распознавания инструментов с фотографий
# Установка: pip install -r requirements_ocr.txt
# Или вручную:
# pytesseract>=0.3.10
# Pillow>=10.0.0

# ============================================================================
# СТАНДАРТНЫЕ БИБЛИОТЕКИ PYTHON (входят в Python, не требуют установки)
# ============================================================================
# asyncio - асинхронное программирование
# logging - логирование
# json - работа с JSON
# re - регулярные выражения
# dataclasses - датаклассы
# enum - перечисления
# pathlib - работа с путями
# datetime - работа с датами
# typing - типизация
# contextlib - контекстные менеджеры
# sqlite3 - SQLite (встроен в Python)
# math - математические функции
# decimal - точные вычисления
# io - работа с потоками ввода-вывода
# ============================================================================

# ============================================================================
# ПРИМЕЧАНИЯ ПО УСТАНОВКЕ
# ============================================================================
#
# 1. БАЗОВАЯ УСТАНОВКА:
#    pip install -r requirements.txt
#
# 2. С OCR (для распознавания фотографий инструментов):
#    pip install -r requirements.txt
#    pip install -r requirements_ocr.txt
#    
#    Также нужно установить Tesseract OCR:
#    - Windows: https://github.com/UB-Mannheim/tesseract/wiki
#    - Linux: sudo apt-get install tesseract-ocr tesseract-ocr-rus tesseract-ocr-eng
#    - macOS: brew install tesseract tesseract-lang
#
# 3. ОПЦИОНАЛЬНО (для более быстрого парсинга HTML):
#    pip install -r requirements_optional.txt
#    Или: pip install lxml
#    
#    ⚠️ ВНИМАНИЕ: На Windows lxml может требовать компилятор C++
#    (Visual Studio Build Tools). Если установка не удалась, бот будет
#    работать с встроенным html.parser из стандартной библиотеки Python.
#    
#    Альтернатива для Windows: скачайте предкомпилированный wheel:
#    https://www.lfd.uci.edu/~gohlke/pythonlibs/#lxml
#
# 4. ПЕРЕМЕННЫЕ ОКРУЖЕНИЯ:
#    Создайте файл .env в корне проекта:
#    TELEGRAM_TOKEN=ваш_токен_бота
#
# 5. БАЗА ДАННЫХ:
#    SQLite создается автоматически при первом запуске
#    Путь: app/storage/cnc.db
#
# ============================================================================
