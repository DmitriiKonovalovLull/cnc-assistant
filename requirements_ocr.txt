# Дополнительные зависимости для OCR (распознавания изображений)
# Установка: pip install -r requirements_ocr.txt

# OCR библиотеки
pytesseract>=0.3.10
Pillow>=10.0.0
opencv-python>=4.8.0,<5.0.0
numpy>=1.24.0,<2.0.0

# Альтернативные OCR (опционально, можно использовать вместо pytesseract)
# easyocr>=1.7.0  # Более точный, но тяжелее
# paddleocr>=2.7.0  # Быстрый и точный

# Примечание: Tesseract OCR должен быть установлен отдельно:
# Windows: https://github.com/UB-Mannheim/tesseract/wiki
# Linux: sudo apt-get install tesseract-ocr tesseract-ocr-rus tesseract-ocr-eng
# macOS: brew install tesseract tesseract-lang
